name: deskilo_push
description: DesKilo's push transport — none (F-Droid build, no Google services).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
